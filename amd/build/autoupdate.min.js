/**
 * AMD module: standalone auto-update for the Quiz Leaderboard page.
 *
 * When the full time-travel module (timetravel.js) is also loaded, it takes
 * ownership of the auto-update controls and this module exits immediately so
 * there is no double-handling. This module only acts as the sole driver of
 * auto-update when time-travel is unavailable (e.g. not enough attempt history
 * to offer the slider).
 *
 * @module     block_quizleaderboard/autoupdate
 * @copyright  2024 Your Name <you@example.com>
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_quizleaderboard/autoupdate",[],function(){return{init:function(){if(document.getElementById("ql-timetravel-controls"))return;const autoupdateControls=document.getElementById("ql-autoupdate-controls"),autoupdateToggle=document.getElementById("ql-autoupdate-toggle"),autoupdateInterval=document.getElementById("ql-autoupdate-interval"),container=document.getElementById("ql-leaderboard-container");if(!autoupdateToggle||!container)return;const quizid=new URL(window.location.href).searchParams.get("quizid"),ajaxurl=autoupdateControls?autoupdateControls.dataset.ajaxurl:null,sesskey=autoupdateControls?autoupdateControls.dataset.sesskey:null;if(!quizid||!ajaxurl||!sesskey)return;let timer=null;function refreshTable(){const params=new URLSearchParams({quizid:quizid,sesskey:sesskey});return fetch(ajaxurl+"?"+params.toString(),{method:"GET",credentials:"same-origin"}).then(r=>{if(!r.ok)throw new Error;return r.text()}).then(html=>{container.innerHTML=html,window.require&&window.require(["block_quizleaderboard/leaderboard"],lb=>lb.init())}).catch(()=>{})}function startTimer(){if(stopTimer(),!autoupdateToggle.checked)return;const secs=Math.max(5,parseInt(autoupdateInterval?autoupdateInterval.value:60,10)||60);timer=setInterval(refreshTable,1e3*secs)}function stopTimer(){timer&&(clearInterval(timer),timer=null)}autoupdateToggle.addEventListener("change",()=>{autoupdateToggle.checked?startTimer():stopTimer()}),autoupdateInterval&&autoupdateInterval.addEventListener("change",startTimer),startTimer()}}});

//# sourceMappingURL=autoupdate.min.js.map